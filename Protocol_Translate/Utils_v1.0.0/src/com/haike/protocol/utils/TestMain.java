package com.haike.protocol.utils;



public class TestMain {

	//public static ApplicationContext ctx = new ClassPathXmlApplicationContext("ApplicationContext.xml");
	// public static void main(String[] args) {
	//
	//
	// ApplicationContext ctx = new ClassPathXmlApplicationContext(
	// "ApplicationContext.xml");
	//
	// Protocol protocol = (Protocol) ctx.getBean("protocol");
	//
	// /**
	// * 将javaBean转化为json对象
	// *
	// */
	// JSONObject jsonObject = JSONObject.fromObject(protocol);
	//
	// /**
	// * 将Str转化为json对象
	// */
	// // JSONObject jsonObject2 = JSONObject.fromObject(str);
	//
	// /**
	// * 反序列化为javaBean对象
	// */
	// // Protocol protocol2 = (Protocol) JSONObject.toBean(jsonObject2,
	// // Protocol.class);
	//
	// // System.out.println(jsonObject.toString());
	//
	// Map<String, Class> classMap = new HashMap<String, Class>();
	// // data,fields 名称需对应各个list变量名
	// classMap.put("data", Data.class);
	// classMap.put("fields", Field.class);
	// classMap.put("comments", Comment.class);
	//
	// protocol = (Protocol) JSONObject.toBean(jsonObject,
	// Protocol.class, classMap);
	//
	// System.out.println(jsonObject.toString());
	// System.out.println(protocol.getData().get(0).getFields().get(0)
	// .getComments().get(0).getDesc());
	//
	// // System.out.println(protocol.getStatus());
	// // System.out.println(protocol.getVersion());
	// // Data data = (Data)protocol.getData().get(0);
	// // System.out.println(protocol.getData().get(0).getCode());
	// // System.out.println(data.toString());
	//
	// }

	// public static void main(String[] args) {
	// System.out.println("进入protocol包");
	// }
}
